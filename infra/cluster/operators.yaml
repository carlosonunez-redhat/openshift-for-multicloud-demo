apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: operators
  namespace: flux-system
spec:
  interval: 1m0s
  sourceRef:
    name: repo
    namespace: flux-system
  path: ./infra/operators
  wait: true
  prune: true
